scheme("Ecosia")
configuration("Testing")
project("firefox-ios/Client.xcodeproj")
device("iPhone16-iOS18.2")
prelaunch_simulator(true)
skip_testing(["BrowserKit/TabDataStoreTests", "TabDataStoreTests"])
# Don't set test_without_building here, we'll control it in the command
output_types("junit")
output_directory("test_output/xml")
skip_package_dependencies_resolution(true)
cloned_source_packages_path("firefox-ios/SourcePackages")
disable_concurrent_testing(true)
number_of_retries(2)
# Don't set build_for_testing here, we'll control it in the command
derived_data_path("DerivedData")
clean(true)
code_coverage(false)
result_bundle(true)
fail_build(false)